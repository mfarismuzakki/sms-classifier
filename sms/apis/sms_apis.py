from django.views.generic import View
from sms.models import *


# class SmsApi(View):
#     @classmethod
#     def get_sender_list(cls, user_id):
        """
        mendapatkan list sms
        """
        # tmp = Sms.objects \
        #     .filter(rece)
